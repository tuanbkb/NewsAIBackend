version: '3.9'

services:
  ollama:
    image: ollama/ollama
    ports:
      - '11434:11434'
    volumes:
      - ollama_data:/root/.ollama

  backend:
    build: .
    ports:
      - '5104:5104'
    depends_on:
      - ollama

volumes:
  ollama_data:
